## What changed, and why

<!-- The why matters more than the what here; the diff already shows the what. -->

## Gates run

- [ ] `npm run typecheck` passes
- [ ] `npm run test:e2e` passes (this does not run in CI; run it locally and
      report the result here)

## Verified by hand

<!--
The e2e suite cannot see a screen, so a visual or UX change can only be
judged by looking at it. If you changed how something looks or behaves,
say what you checked and how (a screenshot, a screen recording, or a plain
description of the steps you took), in both light and dark if the change
touches colour. A behavioural claim ("this is now faster", "this no longer
flickers") needs a number or a screenshot behind it, not just the claim.
-->

## Anything the reviewer should know

<!-- Trade-offs, things you deliberately left out, follow-up you expect. -->
