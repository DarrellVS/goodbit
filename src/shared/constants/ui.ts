/**
 * Sizes both processes have to agree on.
 *
 * The title bar is drawn by the renderer but its height is set on the native
 * overlay by main — if the two disagree, the caption buttons sit off the bar.
 */
export const TITLEBAR_HEIGHT = 40;
